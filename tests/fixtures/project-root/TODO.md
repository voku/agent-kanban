# TODO for Coding Agents

This project uses a split-file Kanban board. The active board cards are located in the [todo/jira/](todo/jira/) directory.

Verify consistency using the command: `make todo_board_verify` or run the standalone package verifier.
